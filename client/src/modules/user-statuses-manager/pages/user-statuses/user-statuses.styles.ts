export const styles = {
  container: {
    paddingTop: '80px',
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fill, minmax(420px, 1fr));',
    justifyItems: 'center',
    gap: '40px'
  }
} as const
